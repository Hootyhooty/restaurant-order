sending message function
   - add a "reply" button below the message but still inside the box in message tab 
   - add a "delete" button next to the "reply" button
   - also add the message format when inside the message tab
        - From: 
          Title:
          Message:
          [Reply] [Delete]
  